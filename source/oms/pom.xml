<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>org.marketcetera</groupId>
        <artifactId>marketcetera</artifactId>
        <version>0.1-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.marketcetera</groupId>
    <artifactId>oms</artifactId>
    <packaging>jar</packaging>
    <name>Marketcetera OMS</name>
    <url>http://maven.apache.org</url>
    <dependencies>
        <dependency>
            <groupId>org.marketcetera</groupId>
            <artifactId>core</artifactId>
        </dependency>
        <!-- JCyclone dependencies -->
        <dependency>
            <groupId>jcyclone</groupId>
            <artifactId>jcyclone-core-marketcetera</artifactId>
            <version>0.1-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>incubator</groupId>
            <artifactId>activemq</artifactId>
        </dependency>

        <!-- dependency on core test code -->
        <dependency>
            <groupId>org.marketcetera</groupId>
            <artifactId>core</artifactId>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>

        <!-- necessary for the ant-junit callout for integration builds -->
        <dependency>
            <groupId>ant</groupId>
            <artifactId>ant-junit</artifactId>
            <version>1.6.5</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- want to run all the tests forked b/c of singleton issues with Jcyclone and OMS -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <forkMode>pertest</forkMode>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>run-integration-tests</id>
                        <phase>integration-test</phase>
                        <configuration>
                            <tasks>
                                <property name="integrationTestReportDir" value="${project.build.directory}/integration-test-reports"/>
                                <taskdef name="junit"
                                         classname="org.apache.tools.ant.taskdefs.optional.junit.JUnitTask">
                                    <classpath>
                                        <path refid="maven.test.classpath"/>
                                    </classpath>
                                </taskdef>

                                <mkdir dir="${integrationTestReportDir}"/>

                                <junit printSummary="yes" haltonerror="true" haltonfailure="true" fork="true" dir=".">
                                    <sysproperty key="basedir" value="."/>
                                    <formatter type="plain" usefile="true"/>
                                    <classpath>
                                        <path refid="maven.test.classpath"/>
                                    </classpath>
                                    <batchtest todir="${integrationTestReportDir}">
                                        <fileset dir="src/test/java">
                                            <include name="**/*IT.java"/>
                                        </fileset>
                                    </batchtest>
                                </junit>

                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
					<configuration>
						<mainClass>org.marketcetera.oms.OrderManagementSystem</mainClass>
                        <arguments>
                            <argument>oms</argument>
                        </arguments>
						<systemProperties>
							<systemProperty>
							  <key>foo</key>      <!-- plugin crashes when there are no values here -->
							  <value>bar</value>
							</systemProperty>
						</systemProperties>
					</configuration>
            </plugin>

        </plugins>
    </build>

</project>
