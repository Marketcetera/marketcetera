<?xml version="1.0" encoding="UTF-8"?>

<!-- Contains basic jms connections configuration -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jms="http://www.springframework.org/schema/jms"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
       http://www.springframework.org/schema/jms http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

  <!-- Connection details -->
  <bean id="externalConnectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
    <property name="brokerURL" ref="brokerURL"/>
    <property name="userName" ref="runtimeUsername"/>
    <property name="password" ref="runtimePassword"/>
  </bean>
  <bean id="delegate" class="org.marketcetera.client.MessagingDelegate">
    <property name="sender" ref="jmsSender"/>
  </bean>

  <!-- Sender Details -->
  <bean id="orsCommandsQueue" class="org.apache.activemq.command.ActiveMQQueue">
    <constructor-arg value="ors-commands"/>
  </bean>

  <bean id="pooledExternalConnectionFactory"
        class="org.apache.activemq.pool.PooledConnectionFactory">
    <property name="connectionFactory" ref="externalConnectionFactory"/>
  </bean>

  <!-- Template to send messages out -->
  <bean id="jmsSender" class="org.springframework.jms.core.JmsTemplate">
    <property name="connectionFactory">
      <ref local="pooledExternalConnectionFactory"/>
    </property>
    <property name="defaultDestination" ref="orsCommandsQueue"/>
    <property name="messageConverter">
      <bean class="org.marketcetera.client.JMSMessageConverter"/>
    </property>
  </bean>

  <!-- Receiver Details -->
  <bean id="orsMessagesTopic" class="org.apache.activemq.command.ActiveMQTopic">
    <constructor-arg value="ors-messages"/>
  </bean>
  <bean id="listener"
        class="org.springframework.jms.listener.adapter.MessageListenerAdapter">
    <property name="messageConverter">
      <bean class="org.marketcetera.client.JMSMessageConverter"/>
    </property>
    <property name="delegate" ref="delegate"/>
  </bean>

  <!-- note that this needs to use the non-pooled connection factory -->
  <bean class="org.springframework.jms.listener.SimpleMessageListenerContainer">
    <property name="connectionFactory" ref="externalConnectionFactory"/>
    <property name="messageListener" ref="listener"/>
    <property name="exceptionListener" ref="delegate"/>
    <property name="destination" ref="orsMessagesTopic"/>
  </bean>
</beans>
