<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <groupId>org.marketcetera</groupId>
    <artifactId>platform</artifactId>
    <version>0.1-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.marketcetera</groupId>
  <artifactId>orderloader</artifactId>
  <packaging>jar</packaging>
  <name>Order Loader</name>
    <description>Loads batches of orders directly into the OMS</description>
  <url>http://maven.apache.org</url>
  <dependencies>
    <dependency>
      <groupId>org.marketcetera</groupId>
      <artifactId>core</artifactId>
    </dependency>
      <!-- dependency on core test code -->
      <dependency>
          <groupId>org.marketcetera</groupId>
          <artifactId>core</artifactId>
          <classifier>tests</classifier>
          <scope>test</scope>
      </dependency>
  </dependencies>
    <build>
        <!-- want to run all the tests forked b/c of singleton issues with Jcyclone and Exchange -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <forkMode>pertest</forkMode>
                </configuration>
            </plugin>
            <!-- try to force a test-jar to be built during jar creation -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>bundle-test-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
