This package contains XSWT examples coded using the old syntax using x:children nodes.
x:children nodes are still supported (and are required if a class happens to be named the
same as an attribute in order to disambiguate the two) but are no longer the 
preferred way to write XSWT code.

