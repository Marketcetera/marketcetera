# Author: anshul@marketcetera.com
# Since: 1.5.0
# Version: $Id$
#
# $License$

# This MUST be encoded in ISO-8859-1. To supply characters that are
# not supported by this encoding, use unicode escapes (\uxxxx).
#
# You may also create a separate file with a native encoding, and then
# use the 'native2ascii' tool of the Java Development Kit to convert
# that file into a this message file in ISO-8859-1. The same tool can
# do the reverse conversion using the '-reverse' command-line option.
#
unexpected_message_to_send.msg=Unable to convert the data object ''{0}'' to a JMS Message as the server does not support this object type. Ensure that the supplied object type is a supported trading message and retry operation.
unexpected_message_received.msg=Unable to convert the JMS message ''{0}'' to a data object as the server does not support this message type or the object contained in it. Ensure that the supplied message is an expected JMS message type and contains a supported object type and retry operation.
