//
// this file is automatically generated
//
syntax = "proto3";
option java_generic_services = true;
option java_package = "org.marketcetera.eventbus.event";
option java_outer_classname = "EventbusRpc";
import "rpc_base.proto";
import "rpc_eventbus_types.proto";

// request events as they are created
message AddEventListenerRequest {
    string sessionId = 1; // session id of the authenticated session
    string listenerId = 2; // listener id of the requester
}

// return events as they are created
message EventResponse {
    Event event = 1;
}

message RemoveEventListenerRequest {
    string sessionId = 1;
    string listenerId = 2;
}

message RemoveEventListenerResponse {
}

// services for eventbus events
service EventbusRpcService {
    rpc login(LoginRequest) returns (LoginResponse); // starts the session
    rpc logout(LogoutRequest) returns (LogoutResponse); // ends the session
    rpc heartbeat(HeartbeatRequest) returns (HeartbeatResponse); // keeps the session alive
    rpc addEventListener(AddEventListenerRequest) returns (stream EventResponse); // requests events as they are created
    rpc removeEventListener(RemoveEventListenerRequest) returns (RemoveEventListenerResponse); 
}
