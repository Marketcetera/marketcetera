<?xml version="1.0" encoding="UTF-8" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.marketcetera</groupId>
    <artifactId>photon-maven</artifactId>
    <version>2.4.1</version>
  </parent>
  <artifactId>photon-rcptarget</artifactId>
  <packaging>pom</packaging>
  <name>Photon RCP Target</name>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>photon-bundle</artifactId>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>photon-test-bundle</artifactId>
    </dependency>
    <dependency>
      <groupId>org.ops4j</groupId>
      <artifactId>peaberry</artifactId>
    </dependency>
    <dependency>
      <groupId>org.ops4j</groupId>
      <artifactId>peaberry.eclipse</artifactId>
    </dependency>
    <dependency>
      <groupId>com.google.inject</groupId>
      <artifactId>guice</artifactId>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>com.mysema.querydsl</groupId>
      <artifactId>querydsl-core</artifactId>
    </dependency>
  </dependencies>
  <profiles>
    <profile>
      <activation>
        <property>
          <!-- 
               - Cleaning the target platform cannot be done while Eclipse is
               - running and building the target platform is time consuming. So
               - it can be disabled with -Dno.photon.target
          -->
          <name>!no.photon.target</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack</id>
                <phase>install</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.marketcetera.photon</groupId>
                      <artifactId>target-platform</artifactId>
                      <version>${mvn.eclipse.version}</version>
                      <type>zip</type>
		      <excludes>eclipse/plugins/org.hamcrest*</excludes>
                    </artifactItem>
                    <artifactItem>
                      <groupId>org.marketcetera</groupId>
                      <artifactId>photon-bundle</artifactId>
                      <version>${project.version}</version>
                      <type>jar</type>
                      <outputDirectory>${mvn.photon.target}/eclipse/plugins/org.marketcetera.thirdparty_${project.version}</outputDirectory>
                    </artifactItem>
                    <artifactItem>
                      <groupId>org.marketcetera</groupId>
                      <artifactId>photon-test-bundle</artifactId>
                      <version>${project.version}</version>
                      <type>jar</type>
                      <outputDirectory>${mvn.photon.target}/eclipse/plugins/org.marketcetera.thirdparty.test_${project.version}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${mvn.photon.target}</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>copy-dependencies</id>
                <phase>package</phase>
                <goals>
                  <goal>copy-dependencies</goal>
                </goals>
                <configuration>
                  <outputDirectory>${mvn.photon.target}/eclipse/plugins</outputDirectory>
                  <!-- Don't copy in marketcetera dependencies, they have their own projects. -->
                  <excludeGroupIds>org.marketcetera</excludeGroupIds>
                  <!-- Don't copy transitive dependencies, they may not be plugins and will be handled by photon-bundle. -->
                  <excludeTransitive>true</excludeTransitive>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <artifactId>maven-clean-plugin</artifactId>
            <configuration>
              <filesets>
                <fileset>
                  <directory>${mvn.photon.target}</directory>
                </fileset>
              </filesets>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
